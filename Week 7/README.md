This submission includes Week 7 Playwright assignments.
1. Refactored all tests using the Page Object Model (POM), removing raw selectors from test files and organizing them into reusable page classes.
2. Implemented hooks (test.describe, beforeEach) to reduce duplication and improve structure.
3. Configured Playwright reporting with screenshots on failure and trace on retry, and verified debugging using an intentionally broken test.
4. Created a custom fixture to provide a pre-configured LoginPage, replacing manual setup in tests.
5. All tests pass successfully.

